{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "notification"],
  "permissions": [
    "core:default",
    "shell:default",
    "notification:default",
    "core:event:default"
  ]
}